[package]
name = "koma-analysis"
description = "Koma semantic analysis: deterministic NarrativeAnalyzer (Phase 11)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
koma-core = { path = "../koma-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
